const mainPageRewievSwiper = new Swiper('.main-page-rewiev-swiper', {
    // If we need pagination
    pagination: {
      el: '.review-swiper-pagination',
      type: 'bullets',
      clickable: true,
    },
  });