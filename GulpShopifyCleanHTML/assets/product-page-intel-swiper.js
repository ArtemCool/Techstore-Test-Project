const productPageIntelSwiper = new Swiper('.product-page-intel-swiper', {
    // If we need pagination
    pagination: {
      el: '.product-page-intel-pagination',
      type: 'bullets',
      clickable: true,
    },
});